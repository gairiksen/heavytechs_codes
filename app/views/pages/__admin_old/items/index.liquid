{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  include 'lib/profile_present_or_redirect', profile: current_profile, allow_uncompleted: true
  include 'lib/must_be_superadmin', profile: current_profile

  function list = 'lib/queries/items/search', limit: 200, lang: context.language

  include 'theme/simple/__admin_old/menu', section: 'items'
  include 'theme/simple/__admin_old/items/index', items: list, role: 'admin'
%}

