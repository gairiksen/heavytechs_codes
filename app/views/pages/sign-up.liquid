{% liquid
  include 'lib/anonymous_or_redirect', user: context.current_user
  assign profile_for = context.params.profile | default: 'mechanic'
  include 'theme/simple/users/new', profile_for: profile_for
%}
