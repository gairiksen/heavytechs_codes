{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  include 'lib/profile_present_or_redirect', profile: current_profile, allow_uncompleted: true
  
  function records = 'lib/queries/profiles/find_my_favourites', keyword: context.params.qkeyword, limit: 15, lang: context.language, user_id: context.current_user.id
%}

{%
  render 'theme/simple/favourites/index', records: records, user_id: context.current_user.id 
%}