{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  function records = 'lib/queries/mechanic/search_initial_admin', keyword: context.params.qkeyword, limit: 15, lang: context.language

  assign this_page = 'admin_mechanic' | default: ''
%}
{%
  render 'theme/simple/admin/mechanic/index', records: records, user_id: context.current_user.id, this_page: this_page  
%}