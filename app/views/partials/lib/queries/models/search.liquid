{% liquid
  assign lang = lang | default: context.language
  graphql r = 'models/search', keyword: keyword, lang: lang
  include 'lib/handle_graphql_response', r: r, name: 'queries/models/search'

  return r.records
%}
