{% liquid
assign lang = lang | default: context.language

graphql r = 'projects/search_project_details', limit: 1, project_id: project_id

include 'lib/handle_graphql_response', r: r, name: 'queries/projects/find_project_details'
return r.records.results.first
%}
