{% liquid
  assign lang = lang | default: context.language

  graphql r = 'contractor/search_blocked_contractor', limit: 20, keyword: keyword, lang: lang, user_id: user_id, id: contractor_profile_id

  include 'lib/handle_graphql_response', r: r, name: 'queries/contractor/search_blocked'

  return r.records
%}
