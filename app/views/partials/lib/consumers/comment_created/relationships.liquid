{% liquid
  assign parent_id = event.target.parent_id | default: event.target.id

  assign now = 'now' | to_date
  function relation = 'lib/commands/relationships/create', name: 'followship', l_id: event.actor.id, r_id: parent_id, l_at: now, r_at: now
%}
