{% liquid
  function audience = 'lib/queries/activities/orders/audience', id: event.object.id, skip_buyer: true
  render 'lib/commands/activities/publish_to_many', uuid: event.uuid, audience: audience, meta: 'order_confirmed'
%}

{% comment %} system:audit {% endcomment %}
{% function act = 'lib/commands/activities/publish', uuid: event.uuid, type: "system", ids: "1" %}
