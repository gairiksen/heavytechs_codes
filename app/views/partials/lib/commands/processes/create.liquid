{%  liquid
  function object = 'lib/commands/processes/create/build', payload: payload, command: command, profile: profile
  function object = 'lib/commands/processes/create/check', object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'processes/create' object: object, selection: 'record_create'
    function status = 'lib/commands/statuses/create', object_id: object.id, name: 'app.statuses.processes.requested', scope: 'app.statuses.processes', profile_id: profile.id
  endif

  return object
%}
