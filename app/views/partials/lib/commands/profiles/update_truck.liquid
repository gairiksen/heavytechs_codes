{% liquid
  function object = 'lib/commands/profiles/update_truck/build', object: object, user_id: user_id, profile: profile

  
    graphql r = 'profiles/update_truck', args: object 

    

  return object
%}
