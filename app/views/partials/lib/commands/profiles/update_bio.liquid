{% liquid
  function object = 'lib/commands/profiles/update_bio/build', object: object, profile: profile, latitude: latitude, longitude: longitude

  
    graphql r = 'profiles/update_bio', args: object 

    

  return object
%}
