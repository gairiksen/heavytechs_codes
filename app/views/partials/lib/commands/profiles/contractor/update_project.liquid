{% liquid
  function object = 'lib/commands/profiles/contractor/update_project/build', object: object, user_id: user_id, object_uuid: object_uuid, geo_location: geo_location, latitude: latitude, longitude: longitude

  graphql r = 'profiles/update_project', args: object 

  return object
%}
