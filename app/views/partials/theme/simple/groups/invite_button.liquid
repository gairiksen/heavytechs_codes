{% function can = "lib/can", actor: current_profile, do: "invite", object: group %}

{% if can.valid %}

<a class="mx-3 button button-primary" href="{% render 'link_to', object: group, action: 'invite' %}">
  Invite member
</a>

{% endif %}