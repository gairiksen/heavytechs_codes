<div class="h-full pt-5 overflow-hidden border rounded-lg bg-panel shadow-sm">
  <a href="/profile/{{ user.slug }}">
    {% render 'theme/simple/ui/avatar', profile: user, size: 24, class: 'w-24 md:w-48 h-24 md:h-48 mx-auto text-2xl md:text-4xl', alt: post.creator.name %}
  </a>

  <div class="flex items-center justify-between p-6">
    {% render 'theme/simple/follow_button', current_profile: current_profile, user: user %}

    <a href="/profile/{{ user.slug }}" class="mb-1 ms-3 font-medium text-content">
      {{ user.name }}
    </a>

    <!--
    <span class="inline-flex text-right items-centertext-sm lg:ml-auto md:ms-0" title="Following">
      <svg class="w-4 h-4 me-1" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24"> <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path> <circle cx="12" cy="12" r="3"></circle> </svg>
      1.2K
    </span>
    -->
  </div>
</div>
