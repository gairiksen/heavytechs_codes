import { Fixture, TestError, TestRunInfo } from './model';
export declare class MultipleHtmlReport {
    private _startTime;
    private _endTime;
    private _userAgents;
    private _testCount;
    private _passed;
    private _skippedTests;
    private _warnings;
    private _currentFixture;
    private _testReportItems;
    private _currentApp;
    get currentFixture(): Fixture | undefined;
    set currentFixture(fixture: Fixture | undefined);
    initialize: (startTime: Date, userAgents: string[], testCount: number) => MultipleHtmlReport;
    finalize: (endTime: Date, passed: number, warnings: string[]) => MultipleHtmlReport;
    createFixture: (name: string, path: string) => MultipleHtmlReport;
    createTest: (name: string, testRunInfo: TestRunInfo, testErrors: TestError[] | undefined) => MultipleHtmlReport;
}
